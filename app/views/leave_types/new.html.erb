<div class="d-flex flex-row ">
    <%= render 'layouts/dashadmin'%>
        <div class="d-flex flex-column">
            <div class="c4">
            <center>
            <h1 class="admin-management-heading">Add New Leave </h1>
            <%= render "remote_modal", title: "New Leave" do %>
            <%= render "form", leave_type: @leave_type %>
            <% end %>
            </center>
        </div>
</div>